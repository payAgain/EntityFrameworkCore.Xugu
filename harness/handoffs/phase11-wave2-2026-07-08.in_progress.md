# Phase 11 Wave 2 Handoff — JSON Provider 11.109a

> **日期**：2026-07-08  
> **状态**：`in_progress`（11.109a done；11.109b–d todo）  
> **Wave**：W2（11.109）  
> **前置**：W1 done ✅

---

## 11.109a 交付（done）

| 项 | 结果 |
|----|------|
| `XuguJsonTypeMapping` | `Storage/Internal/XuguJsonTypeMapping.cs` — CLR `string` → Xugu `JSON` LOB |
| `XuguTypeMappingSource` 注册 | `JSON` store type + `FindMapping` 优先解析 |
| Migrations DDL | `HasColumnType("JSON")` → `CREATE TABLE … JSON` |
| 单元测试 | `JsonColumnTests`、`TypeMappingSourceTests`、`MigrationSqlGeneratorExtensionTests` |

**测试增量**：+6 列测（867 总计，含新增 JSON 映射断言）

---

## 验证

```powershell
dotnet build Xugu.EFCore.Xugu.sln -c Release   # PASS
harness/scripts/verify.ps1                      # PASS
dotnet test --filter "FullyQualifiedName~JsonColumn|TypeMappingSource|MigrationSqlGenerator"  # 46 PASS
```

> 全量 `dotnet test` 在并行实库压力下偶有 MonsterFixup/DesignTime 抖动（与 JSON 变更无关）；隔离重跑 PASS。

---

## 下一项（11.109b）

- JSON 路径 `->` / `->>` LINQ 翻译（`JsonScalarExpression`）
- 参考：`reference/sql/operators/json-operators/`、`MySqlJson*` translators（架构 only）

---

## 参考

- `harness/contracts/sql-dialect.contract.md` §JSON 列
- `docs/LIMITATIONS.md` §JSON 列
- `E:\BaiduSyncdisk\docs\content\reference\sql\datatype\json.md`
